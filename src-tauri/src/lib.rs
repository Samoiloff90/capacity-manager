//! Native lifecycle and shared production IPC for local project folders, plus the
//! narrow native command that saves a report workbook built by the frontend.
pub mod project_store;
pub mod report_export;
